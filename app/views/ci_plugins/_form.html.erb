<%= form_tag ci_plugins_path, class: 'c-hide fis-ci-plgins-form' do %>


  <%= ci_plugins.each do |ci_plugin| %>

    <% if ci_plugin.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(ci_plugin.errors.count, "error") %> prohibited this ci_plugin from being saved:</h2>

        <ul>
        <% ci_plugin.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>


    
  <% end %>
  
  <div class="actions">
    <%= submit_tag "submit", class: "fis-ci-plgins-form-submit-btn"%>
  </div>

<% end %>
