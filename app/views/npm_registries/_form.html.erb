<%= form_for(npm_registry) do |f| %>
  <% if npm_registry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(npm_registry.errors.count, "error") %> prohibited this npm_registry from being saved:</h2>

      <ul>
      <% npm_registry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :label %>
    <%= f.text_field :label, class: "c-input" %>
  </div>

  <div class="field">
    <%= f.label :registry_url %>
    <%= f.text_field :registry_url, class: "c-input c-block-input" %>
  </div>

  <div class="field">
    <%= f.label :checked %>
    <%= f.check_box :checked %>
  </div>

  <div class="actions">
    <%= f.submit class: "c-btn"%>
  </div>
<% end %>
