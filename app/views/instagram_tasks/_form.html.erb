<%= form_with(model: instagram_task, local: true) do |form| %>
  <% if instagram_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instagram_task.errors.count, "error") %> prohibited this instagram_task from being saved:</h2>

      <ul>
      <% instagram_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :instagram_task_user_id %>
  </div>

  <div class="field">
    <%= form.label :task_type %>
    <%= form.text_field :task_type, id: :instagram_task_task_type %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :instagram_task_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
