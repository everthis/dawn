<%= form_for(gist) do |f| %>
  <% if gist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gist.errors.count, "error") %> prohibited this gist from being saved:</h2>

      <ul>
      <% gist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label :hasAnswer %>
    <%= f.check_box :hasAnswer %>
  </div>

  <div class="field">
    <%= f.label :answer %>
    <%= f.text_area :answer %>
  </div>

  <div class="actions">
    <%= f.submit class: 'c-form-submit' %>
  </div>
<% end %>
